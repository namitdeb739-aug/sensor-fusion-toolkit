"""Entry point for sensor-fusion-toolkit."""


def main() -> None:
    """Run the main application.

    This is the CLI entry point configured in ``pyproject.toml``
    under ``[project.scripts]``.
    """
    print("Hello from sensor-fusion-toolkit!")


if __name__ == "__main__":
    main()
